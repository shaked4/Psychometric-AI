# Copy to .env.local and fill in your key to enable the AI tutor.
# Without it, /api/tutor degrades to a static fallback message — the app
# stays fully functional in local/offline dev.
ANTHROPIC_API_KEY=
